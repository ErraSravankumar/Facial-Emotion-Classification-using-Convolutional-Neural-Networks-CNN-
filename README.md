# Facial-Emotion-Classification-using-Convolutional-Neural-Networks-CNN-
This project uses a Convolutional Neural Network (CNN) to classify human facial emotions from images. The model identifies two expressions: Angry and Happy. Using convolution and pooling layers, the CNN learns facial features and extracts patterns from images to perform emotion classification.
